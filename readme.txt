Mon Idée (base exercice du TP)

Créer des formulaires, dans lesquels nous ajoutons autant de questions souhaitées (sans possibilité de suppression)
à l'ajout du formulaire, les données transits dans un object localStorage afind d'être lues sur une autre page (selecteur de formulaire en idée mais non fonctionnelle),
 Afficher les différentes questions + input type du formulaire choisi

Etat des lieux en fin de journée : impossible de finir à temps le dev de génération des n-inputs X n type d'input sur la pager 2
(je n'ai pas boucler et traité suffisament proprement les données pour voir croiser les infos avec des objects).

les console.log présents sur la page 2 sont faits sur une base de données statiques en HTML (c'est celles çi que je voulais transformer)

La navigation fixée sur la droite permet de clear() le localStorage afin de ne pas se retrouver avec un objet remplis de données tests.

